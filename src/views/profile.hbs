<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    <h3>Welcome {{user.fullname}}</h3>
                    <h4>{{user.username}}</h4>
                    {{#if user.isAdmin}}
                    <a href="/links" class="btn btn-primary m-4">
                        List your links   
                          <a href="/Manager" class="btn btn-primary m-4">
                        List your Manager 
                          <a href="/Worker" class="btn btn-primary m-4">
                        List your Worker  
            
                    </a>
                    {{/if}}
                    {{#if user.isManager}}
                    <a href="/links" class="btn btn-primary m-4">
                        List your links  
                          <a href="/Worker" class="btn btn-primary m-4">
                        List your Worker  
            
                    </a>
                    {{/if}}
                {{#if user.isWorker}}
                    <a href="/links" class="btn btn-primary m-4">
                        List your links  
                    
            
                    </a>
                    {{/if}}
                </div>
            </div>
        </div>

    </div>
</div>